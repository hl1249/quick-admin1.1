# 线上环境
ENV = 'production'

# base路径
VITE_BASE_PATH = '/'

# 导出路径
VITE_OUT_DIR = 'dist'

# 线上环境接口地址 - 'getCurrentDomain:表示获取当前域名'
VITE_AXIOS_BASE_URL = 'getCurrentDomain'

# websocket 基础地址；默认会根据 VITE_AXIOS_BASE_URL 自动推导，只有和接口域名不一致时才需要手动指定
# VITE_SOCKET_BASE_URL = 'getCurrentDomain'
